<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>TinyApp</title>
	<style>
		* {
			list-style: none;
		}
	</style>
</head>
<body>
	<% include partials/_header %>
	<ul>
		<% for (item in urls) { %>
			<li>Long URL: <%= urls[item]['longURL'] %>
				<form method="POST" action="/urls/<%= item %>/delete">
						<a href="/u/<%= item %>"><%= item %></a>
						<input type="submit" value="Delete">
						<a href="/urls/<%= item %>">Edit</a>
				</form>
			</li>
		<% } %>
	</ul>
</body>
</html>